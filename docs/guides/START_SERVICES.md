# How to Start the Services

## Quick Start

The API is now running! To start the frontend:

### Terminal 2 - Start Frontend:
```bash
cd test-harness/dashboard-frontend
npm run dev
```

Wait until you see:
```
  VITE v5.x.x  ready in xxx ms
  ➜  Local:   http://localhost:5173/
```

### Then Open Browser:
Open http://localhost:5173

## Current Status

✅ **API**: Running on http://localhost:3001
⏳ **Frontend**: Start it in a separate terminal

## If API Stops

To restart the API:
```bash
cd test-harness/dashboard-api
npm run start:dev
```

Look for: `🚀 Heimdall Dashboard API running on http://localhost:3001`
